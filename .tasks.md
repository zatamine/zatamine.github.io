# Tasks — zatamine theme rebuild (2026-08-24)

## Plan
- [x] Inspect existing repo structure, configs, scaffold theme
- [x] Write `implementation_plan.md` and present to user

## Site config & data
- [/] Update `config/_default/hugo.yaml` — title/description, theme zatamine, Chroma (noClasses + dark style), tags taxonomy only, menus (About / What I Do / Blog / Contact), params (author, email, socials)
- [ ] Slim `config/development/hugo.yaml` to env-specific overrides only
- [ ] Create `data/services.yaml` — 4 service cards w/ inline SVG icons + tech chips
- [ ] Create `data/status.yaml` — status line, uptime, ping, availability badge, stack badges

## Content
- [ ] Write 3 deep-dive posts (exact spec titles) under `content/posts/`, tagged, with Go/YAML/Bash blocks for Chroma + TOC demo
- [ ] Fix & rewrite `content/about/index.md` (currently broken duplicate front matter)

## Theme layouts (`themes/zatamine/layouts/`)
- [ ] `theme.toml` metadata refresh
- [ ] `_default/baseof.html`, `index.html` (home), `_default/list.html` (archive + tags/term views), `_default/single.html` (post w/ sticky TOC + author bio; non-post single fallback)
- [ ] Partials: `head.html`, `nav.html`, `hero.html`, `services.html`, `recent-posts.html`, `contact.html`, `footer.html`, post-meta/tag-chips as needed
- [ ] Remove superseded scaffold partials/pages (`home.html`, `header.html`, `menu.html`, `terms.html`, split head partials, stray theme `public/`)

## Assets & validation
- [ ] `assets/css/main.css` — full design system (vars, reset, grid, status pulse, `.prose`, responsive)
- [ ] `assets/js/main.js` — mobile nav toggle only
- [ ] Build: `hugo --minify` + error/warning scan; fix anything surfaced
